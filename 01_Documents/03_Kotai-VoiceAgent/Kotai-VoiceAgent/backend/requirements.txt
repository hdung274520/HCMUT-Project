# Kotai Voice Agent — Python dependencies (verified working set)
# Install into a fresh Python 3.11 environment:  pip install -r requirements.txt
# (On a GPU machine, `pip install torch` on Linux already pulls the CUDA build.)

# --- LiveKit real-time agent framework + plugins ---
# Keep these four on the SAME version.
livekit-agents==1.2.11
livekit-plugins-openai==1.2.11          # OpenAI-compatible LLM client (used to talk to Ollama)
livekit-plugins-silero==1.2.11          # Silero VAD (voice activity detection)
livekit-plugins-turn-detector==1.2.11   # end-of-utterance / turn detection model

# --- Speech-to-Text: faster-whisper (CTranslate2), runs locally on CPU ---
faster-whisper==1.2.0
ctranslate2==4.6.0

# --- Text-to-Speech: Kokoro (local neural TTS, uses Kokoro-TTS-Local/) ---
kokoro==0.9.4
misaki==0.9.4                           # G2P / phonemizer used by Kokoro

# --- ML runtime ---
torch==2.7.0
torchaudio==2.8.0
numpy>=2.0,<3                           # NumPy 2.x — do NOT let a dependency pull <2 (breaks torch ABI)

# --- Audio + misc utilities ---
pydub==0.25.1
soundfile==0.13.1
openai==1.108.1                         # OpenAI Python SDK (client object passed to livekit openai.LLM)
python-dotenv==1.0.1
httpx==0.28.1
pydantic>=2.0
huggingface-hub==0.35.3
